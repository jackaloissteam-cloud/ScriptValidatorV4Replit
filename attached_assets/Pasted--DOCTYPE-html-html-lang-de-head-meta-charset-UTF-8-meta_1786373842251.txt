<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>App Portal</title>
  <style>
    :root {
      --bg: #0f172a;
      --card-bg: #1e293b;
      --text: #f8fafc;
      --text-muted: #94a3b8;
      --accent: #38bdf8;
      --border: #334155;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    
    body {
      font-family: system-ui, -apple-system, sans-serif;
      background-color: var(--bg);
      color: var(--text);
      padding: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    header {
      margin-bottom: 2.5rem;
      border-bottom: 1px solid var(--border);
      padding-bottom: 1rem;
    }

    h1 { font-size: 1.8rem; font-weight: 600; }
    p.subtitle { color: var(--text-muted); margin-top: 0.25rem; }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 1.5rem;
    }

    .card {
      background-color: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 1.5rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .card-title { font-size: 1.2rem; font-weight: 500; }
    
    .badge {
      font-size: 0.75rem;
      background: rgba(56, 189, 248, 0.1);
      color: var(--accent);
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      border: 1px solid rgba(56, 189, 248, 0.2);
    }

    .btn {
      display: inline-block;
      text-align: center;
      background-color: var(--accent);
      color: #000;
      font-weight: 600;
      padding: 0.6rem 1rem;
      border-radius: 6px;
      text-decoration: none;
      transition: opacity 0.2s;
    }

    .btn:hover { opacity: 0.9; }

    /* iFrame Container fÃ¼r eingebettete Vorschau */
    .preview-container {
      margin-top: 1rem;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid var(--border);
      height: 400px;
    }

    iframe {
      width: 100%;
      height: 100%;
      border: none;
    }
  </style>
</head>
<body>

  <header>
    <h1>Mein App-Portal</h1>
    <p class="subtitle">Zentrale Ãœbersicht aller aktiven Anwendungen</p>
  </header>

  <main class="grid">

    <!-- Karte: Script Validator -->
    <article class="card">
      <div class="card-header">
        <h2 class="card-title">Script Validator</h2>
        <span class="badge">Aktiv</span>
      </div>
      
      <p style="color: var(--text-muted); font-size: 0.9rem;">
        Validierungstool fÃ¼r Skripte. Gehostet auf Hercules / Vercel.
      </p>

      <!-- Direktlink zur App -->
      <a 
        href="https://script-validator-083099.onhercules.app/" 
        target="_blank" 
        rel="noopener noreferrer" 
        class="btn"
      >
        App Ã¶ffnen â†—
      </a>

      <!-- Optional: Direkt eingebettetes Fenster -->
      <div class="preview-container">
        <iframe 
          src="https://script-validator-083099.onhercules.app/" 
          title="Script Validator Vorschau"
          loading="lazy"
        ></iframe>
      </div>
    </article>

  </main>

</body>
</html>